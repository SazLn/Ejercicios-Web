<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="Styles.css">
    <title>🥶 Formulario de Contacto</title>
    <script src="https://kit.fontawesome.com/6dbb48a263.js" crossorigin="anonymous"></script>
</head>

<body>
    <section class="contenedor-principal">
        <h2>¡Contáctanos!</h2>

        <!-- El atributo [method = "post"] envía los datos del formulario de manera segura sin mostrarlos en la URL de la página. -->
        <form action="#" class="formulario" method="post">
            
            <div class="contenedor-inputs">
                <label for="Nombre"></label>
                <input type="text" name="nombre" id="Nombre" placeholder="Tu nombre">

                <label for="Email"></label>
                <input type="email" name="email" id="Email" required placeholder="Tu correo electrónico">
            </div>

            <label for="Mensaje">Mensaje:</label>
            <textarea name="mensaje" id="Mensaje"></textarea>

            <button type="submit">Enviar</button>
        </form>
    </section>
</body>

</html>

---------------------------------------------------------------

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;

    font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
    padding: 10px;
    background-color: rgb(245, 248, 250);
}

/* Estilos para el contenedor del formulario */
.contenedor-principal {
    max-width: 700px;
    background-color: white;
    border-radius: 10px;
    padding: 20px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
;
}
.contenedor-principal h2 {
    text-align: center;
    margin-bottom: 20px;
    color: rgb(23, 23, 43);
}

/* Estilos para el formulario */
.contenedor-principal form {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 20px;
}

.contenedor-inputs {
    display: flex;
    gap: 10px;
    
}
.contenedor-principal input, textarea {
    width: 100%;
    font-size: 1.1em;
    padding: 10px;
    border-radius: 5px;
    outline: none;
    border: 1px solid rgb(167, 167, 167);
    transition: border 0.3s ease, box-shadow 0.3s ease;
}
.contenedor-principal input:focus,
.contenedor-principal textarea:focus {
    border: 1px solid rgb(74, 122, 212);
    box-shadow: 0 0 5px rgba(74, 122, 212, 0.89);
}

.contenedor-principal textarea {
    resize: vertical; /* 'resize' es una propiedad singular de <textarea> y sirve para que el usuario pueda cambiar el tamaño del <textarea> */
    min-height: 100px;
}

button {
    align-self: center;
    width: 80%;
    padding: 10px;
    border-radius: 5px;
    border: none;
    background-color: rgb(74, 122, 212);
    color: white;
    font-size: 1.2em;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: rgb(53, 108, 211);
}

/* Media Query para cuando el tamaño de pantalla es pequeño */
@media (max-width: 600px) {
    .contenedor-inputs {
        flex-direction: column;
        gap: 10px;
    }
}
